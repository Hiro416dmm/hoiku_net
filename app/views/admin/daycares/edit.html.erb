<div class="container">
  <div class="row mb-3">
    <div class="col">
      <h4 class="ml-5">保育施設編集</h4>
    </div>
  </div>
  <%= form_with model: [:admin, @daycare], url: admin_daycare_path(@daycare.id), method: :patch do |f| %>
  <%= render :partial => 'form', :locals => {:f => f, :edit_flag => true} %>
  <% end %>
</div>
